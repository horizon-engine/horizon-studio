import type { HandleType } from "./types";
export declare const handleValidTypes: Map<string, Map<string, HandleType | null>>;
export declare let store: {
    clearErrors: boolean;
    updateFlow: boolean;
};
